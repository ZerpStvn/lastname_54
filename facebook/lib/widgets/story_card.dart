import 'package:flutter/material.dart';

class StorCard extends StatelessWidget {
  const StorCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
